import { NavLink } from 'react-router-dom';import { Home,MessageCircle,Network,UserRound,ClipboardList } from 'lucide-react';import { cn } from '@/lib/utils';
const items=[['/app/home','Beranda',Home],['/app/needs','Kebutuhan',ClipboardList],['/app/messages','Pesan',MessageCircle],['/app/units','Jajaran',Network],['/app/profile','Profil',UserRound]] as const;
export const BottomNavigation=()=> <nav className="fixed inset-x-0 bottom-0 z-40 border-t border-slate-200 bg-white/95 px-2 pb-[env(safe-area-inset-bottom)] pt-2 backdrop-blur"><div className="mx-auto grid max-w-2xl grid-cols-5 gap-1">{items.map(([to,label,Icon])=><NavLink key={to} to={to} className={({isActive})=>cn('flex flex-col items-center gap-1 rounded-2xl px-2 py-2 text-xs font-bold text-slate-500',isActive&&'bg-skyforce-50 text-skyforce-700')}><Icon size={20}/>{label}</NavLink>)}</div></nav>;
