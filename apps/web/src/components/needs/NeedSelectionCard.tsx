import { Card,CardDescription,CardTitle } from '@/components/ui/card';import { needOptions, type NeedType } from '@/data/catalog';
export const NeedSelectionCard=({onSelect}:{onSelect:(need:NeedType)=>void})=><section className="space-y-3"><div><h2 className="text-2xl font-black text-slate-950">Apa kebutuhan Anda hari ini?</h2><p className="text-sm text-slate-500">Pilih kebutuhan, Telehealth AU akan menyesuaikan pertanyaan dan rekomendasi.</p></div><div className="grid gap-3 md:grid-cols-2">{needOptions.map(n=><Card key={n.id} role="button" onClick={()=>onSelect(n.id)} className="cursor-pointer p-4 transition hover:-translate-y-0.5 hover:border-skyforce-500"><div className={`mb-3 inline-flex rounded-full px-3 py-1 text-xs font-bold ${n.accent}`}>{n.title.split(' ').slice(2,5).join(' ')||n.title}</div><CardTitle className="text-base">{n.title}</CardTitle><CardDescription>{n.description}</CardDescription></Card>)}</div></section>;
