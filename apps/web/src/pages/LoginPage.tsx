import { useForm } from 'react-hook-form';import { zodResolver } from '@hookform/resolvers/zod';import { useNavigate,Link } from 'react-router-dom';import { Button } from '@/components/ui/button';import { Card,CardDescription,CardTitle } from '@/components/ui/card';import { Input } from '@/components/ui/input';import { loginSchema } from '@/lib/schemas';import { useAuthStore } from '@/stores/appStores';import type { z } from 'zod';
export const LoginPage=()=>{const nav=useNavigate();const login=useAuthStore(s=>s.login);const {register,handleSubmit}=useForm<z.infer<typeof loginSchema>>({resolver:zodResolver(loginSchema)});return <main className="flex min-h-screen items-center justify-center bg-skyforce-50 p-4"><Card className="w-full max-w-md"><CardTitle className="text-2xl">Telehealth AU</CardTitle><CardDescription>Masuk dengan NRP/NIP/NIK/email/identitas internal.</CardDescription><form className="mt-5 space-y-3" onSubmit={handleSubmit(()=>{login('dummy-jwt');nav('/app/home')})}><Input placeholder="Identitas" {...register('identifier')}/><Input type="password" placeholder="Kata sandi" {...register('password')}/><Button className="w-full">Masuk</Button><Button asChild variant="outline" className="w-full"><Link to="/register">Daftar pasien</Link></Button></form></Card></main>};
